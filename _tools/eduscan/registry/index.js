/**
 * EduScan - Registry Module
 *
 * Exports registry generators and utilities.
 */

const ModuleRegistryGenerator = require('./module-registry');

module.exports = {
    ModuleRegistryGenerator
};
