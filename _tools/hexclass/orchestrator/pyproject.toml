[project]
name = "hex-ai-orchestrator"
version = "0.6.2"
description = "Hexworth Prime AI orchestration layer — Dr. Hex's constrained shell"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "httpx>=0.27",
    "pydantic>=2.7",
    "redis[hiredis]>=5.0",        # v0.6.1 — bounded conversation memory
    "psycopg[binary]>=3.1",        # v0.2.0 — pgvector RAG (was implicit before)
]
