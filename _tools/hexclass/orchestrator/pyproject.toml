[project]
name = "hex-ai-orchestrator"
version = "0.0.1"
description = "Hexworth Prime AI orchestration layer — Dr. Hex's constrained shell"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "httpx>=0.27",
    "pydantic>=2.7",
]
