# SITREP — live session cursor

> Shorthand brief of what we are working on RIGHT NOW. Overwritten in place at every
> meaningful transition (task start/finish, decision point, pre/post deploy). On session
> start or after a restart/crash: read this FIRST, then the linked memory files for depth.

<!-- AUTO:BEGIN (regenerated by update-sitrep-auto.sh via Stop hook; do not hand-edit) -->
**Auto-updated:** 2026-07-31 23:23 EDT | branch: master | dirty files: 44

### Latest commits (auto)
- e4dd5a81d chore(hub): archive before removing -- we do not destroy
- 5a28ab24b chore(hub): script to unregister the capstone from the Projects shelf (NOT RUN)
- 32f8ea3ce docs(sitrep): the capstone is a lab on a projects shelf; dedup gated but held
- 1793c096a fix(hub): normalise hrefs, so the backstop actually backstops
- f2596ca5c docs(bugs): date entries by the repo's local convention, not the UTC clock
<!-- AUTO:END -->

**Manual section updated:** 2026-07-31 — operator driving. Delete protocol hardened. Capstone taxonomy call pending.

## NOW — operator is in the loop. Do NOT start marathon tasks; he is reviewing.

### THE CAPSTONE IS A LAB, NOT A PROJECT (operator ruling)
It renders TWICE on the live Cloud Master hub. Root cause is NOT a de-dup bug -- it is a
mislabel. Structurally identical to the five live labs (4 checks, SandboxLauncher, same dir,
same `.lab.html` naming, same two-harness + qc-lab-mode pattern). The platform's real project
system has never heard of it: no `/projects/` landing page, zero refs in qc-projects.py.
"Project" was a label on the shelf, nothing more.
DECISION: keep it as a LAB. Remove the curated Firestore Projects entry so it renders once,
under Labs. Cloud Master's Projects shelf then reads 0, which is TRUE -- no project is built.
NOT YET RUN. Needs operator authorization for that specific write (rule 10).

### DELETE PROTOCOL -- hardened tonight, operator-driven
- `mv` was NOT in the permission deny-list, only `rm`. Added `Bash(mv *)` + `Bash(mv -f *)`
  (12 deny rules now). Settings archived + verified before editing.
- The archive rule CONTRADICTED ITSELF: the refinement said "never mv", the How-to-apply line
  three rows below still said `mv`. I followed the instruction line and used mv ~320 times
  tonight. Nothing lost (verified: 0 zero-byte, contents valid) -- luck, not method. Fixed.
- **ONE authorized delete process:** `_tools/firestore/safe-delete.js` (470af4637). Surgical
  `arrayRemove` (not read-modify-write -- the operator caught that window himself), fails safe on
  ambiguity, archive-verified-or-abort, dry-run default, refuses field/doc deletes outright.
- `functions/unregister-capstone-project.js` retired to an inert stub. Original archived
  byte-identical + in git history. No rm, no mv in the change.
- **OPEN, put to the operator:** index.js (5 delete calls, deployed runtime, probably legit),
  plus account-merge.js / bulk-unlock.js / hackerman-reset.js / fix-clh-022-fixnote — same class
  as the retired one-off, unaudited. Audit them or scope the rule to manual deletes?

### DEDUP FIX -- gated, NOT deployed, and deliberately so
Nancy PROCEED (d636f1194), Chris PASS (1793c096a) after blocking twice, both correctly. His
second block was the good one: my comment claimed the probe was the regression net for
normalization drift; he built the fixture and proved the probe shared the exact raw-string blind
spot it was meant to backstop. `normHref()` now in both.
HELD because shipping it ALONE keeps the mislabelled Projects card and drops the honest Labs
one. Must land with or after the Firestore removal, never before.

### Also still gated on the operator
`reclaim-idle-slots.py --apply`. Pool 30 / **29 bound / 1 free**. Script is on the wrong host
(claim_service.py is on bc2). Nancy: layer a Firebase-identity filter first -- the resource test
cannot tell a dead slot from a student mid-lab.

### Task board
- [ ] OPERATOR: authorize the capstone Firestore removal -> then deploy the de-dup fix
- [ ] OPERATOR: audit the 4 remaining one-off delete scripts, or scope the rule
- [ ] OPERATOR: reclaim --apply
- [ ] #282 build P2 "Survive the Drift" -- the real fix for an empty Projects shelf
- [ ] #280 ~31 unreachable files are described to the AI assistant
- [ ] BUG-077 [P2] attempt-scope the capstone baseline

### Standing hazards
- A cron heartbeat fires every ~30 min telling me to take new tasks. When the operator is mid-
  conversation, HOLD -- a live instruction outranks a timer. He was explicit about this.
- Token cost is a real constraint he has raised directly. Short answers. Answer the question
  asked, not the adjacent one.
