# SITREP — live session cursor

> Shorthand brief of what we are working on RIGHT NOW. Overwritten in place at every
> meaningful transition (task start/finish, decision point, pre/post deploy). On session
> start or after a restart/crash: read this FIRST, then the linked memory files for depth.

<!-- AUTO:BEGIN (regenerated by update-sitrep-auto.sh via Stop hook; do not hand-edit) -->
**Auto-updated:** 2026-07-31 20:31 EDT | branch: master | dirty files: 43

### Latest commits (auto)
- 558db1349 docs(projects): P1 is MIXED-TIER graded, not fully server-side
- 0e30e6760 test(smoke): walk real anchors to answer 'can a student click their way here'
- ac4c66d9e docs(bugs): BUG-075 and BUG-076 deployed-verified with live evidence
- 1d5a63b1d fix(openstack): the Stage 4 capstone was built, gated, and unreachable
- 4abd487a3 docs(sitrep): lightning held at Chris gate on a 4x undermeasurement, corrected
<!-- AUTO:END -->

**Manual section updated:** 2026-08-01 — lightning SHIPPED + verified on prod. Now on capstone reachability; Nancy PAUSE being answered by measurement.

## NOW — Cloud Master capstone was ORPHANED. Link committed, NOT deployed, pending bc1 gate.

### SHIPPED AND VERIFIED ON PRODUCTION (deploy 4abd487a3)
Hub lightning + birds removed + `/houses/hub/**` cache `max-age=3600` -> `no-store`.
Measured ON hexworth.com, not the preview channel:
  storm worst-case delta 0.0735 vs WCAG 2.3.1 threshold 0.10 (27% margin), 1 cell, reduced-motion off
  scoping 18/18 (aws-ccp + openstack controls unchanged) | contrast 6/6 AA at peak flash
  perf 60.2fps idle AND pointer on 6x-throttled CPU | BUG-075 + BUG-076 confirmed live
Chris BLOCKED round 1: the probe sampled a fixed window at the CSS gradient centre (peak ALPHA)
and under-reported the flash 4x. Rewritten (415e9306a) to scan every 10x10 block. Three
independent measurements now agree within 0.0006.

### THE CAPSTONE FIND
`cloud-openstack-project-iac.lab.html` -- finished page, BOTH QC harnesses, coverage-gated
`project` mode (ids 25-28), catalog entry -- had ZERO links from the OpenStack hub. Each of its
five sibling live labs had exactly one. Built, gated, unreachable. Link added (1d5a63b1d).
The strict-orphan-scanner HAD caught it (cloud orphans 6 -> 5); nobody reads a 420-entry JSON.

### NANCY: PAUSE — the card is fine, what it EXPOSES is the question
Answered by reading bc1 lab-manager/server.js directly (not by argument):
  26  CLOUD_CHECKS fn   reads the live cloud                NOT forgeable
  27  CLOUD_CHECKS fn   server-side pre-teardown baseline   NOT forgeable
  25  SANDBOX_CHALLENGES cmd  student container             forgeable alone
  28  SANDBOX_CHALLENGES cmd  greps a student-written file  forgeable alone
Design holds -- fake the manifest and 26 fails; never destroy and 27 fails -- but
`ProjectsData.js` claimed "gated live on bc1 (CLOUD_CHECKS 25-28)", false for half. Corrected
(558db1349). hasPage:false is CORRECT and stays: /projects/ links `/projects/<id>.html`, which
does not exist; flipping it manufactures a 404.

### IN FLIGHT
`bash qc-lab.sh project` running on bc1 -> `/home/eq1/qc-project-2026-08-01.log`.
Adversarial stage: cheat B rejected by 25, cheat C rejected by 26. Awaiting cheat D + walkthrough.
DO NOT deploy the capstone link until this transcript is green.

### Task board
- [~] bc1 capstone gate transcript (Nancy's central ask) -- RUNNING
- [ ] #269 checks 25/28 forgeable in isolation -- accept+label, or move 28 server-side
- [ ] #268 orphan findings invisible (stdout prints cluster SAMPLES only)
- [ ] #271 Projects hub cannot link the capstone (id + tree mismatch)
- [ ] #270 bc1 server.js stale comment still lists 27 as container-side
- [ ] 5 cloud orphans: 4 look reachable via aws-ccp hub -> aws-services lab; VERIFY with walker
- [ ] #261 BUG-073 127 achievement definitions -- OPERATOR/curriculum call

### Standing hazards
- Preview channel = different origin; `hexworth_house` does not travel. Send `sorting.html` first.
- Every hub probe injects `hexworth_house`, so no probe can catch an access-gate failure.
- ELEVEN bad checks written today. All the same shape: predicting a string instead of reading it.
  New tool: `_tools/eduscan/smoke/reachability-walk.js` (validated on + and - controls).
