# SITREP — live session cursor

> Shorthand brief of what we are working on RIGHT NOW. Overwritten in place at every
> meaningful transition (task start/finish, decision point, pre/post deploy). On session
> start or after a restart/crash: read this FIRST, then the linked memory files for depth.

<!-- AUTO:BEGIN (regenerated by update-sitrep-auto.sh via Stop hook; do not hand-edit) -->
**Auto-updated:** 2026-07-31 21:35 EDT | branch: master | dirty files: 43

### Latest commits (auto)
- 8852355b6 fix(qc-harness): wall was still minting a new pool slot on every run
- e117ce28f fix(smoke): bound the walk, and never report a truncated crawl as a dead end
- 97eae38fc docs(bugs): BUG-077 to P2 -- Nancy rejected my P3 and was right
- 58f36482e docs(bugs): BUG-077 corrected P1 -> P3; the P1 evidence was my harness's own debris
- f70510b98 feat(eduscan): standing check for catalog entries nothing links to
<!-- AUTO:END -->

**Manual section updated:** 2026-08-01 — hub double-render FIXED (Chris PASS, Nancy pending). BUG-077 P2. Two actions gated on the operator.

## NOW — hub de-dup fix awaiting Nancy; capstone containment BLOCKED on operator authorization.

### SHIPPED + VERIFIED ON PRODUCTION (4abd487a3)
Hub lightning, birds out, `/houses/hub/**` cache -> `no-store`. Storm delta 0.0735 vs WCAG 0.10
(27% margin), scoping 18/18, contrast 6/6 AA, 60.2fps. BUG-075 + BUG-076 live.

### GATED ON THE OPERATOR — cannot proceed without explicit authorization
1. **Remove `hubRegistry/cloud-master.sections.projects` capstone entry.** Direct production
   Firestore write, CLAUDE.md rule 10. Nancy stated her review is NOT that authorization.
2. **`reclaim-idle-slots.py --apply`.** Pool measured 30 slots / 29 bound / **1 free** -- next
   student or QC run gets a 503. Nancy PAUSED: the resource test (creds/servers/volumes = 0)
   cannot tell a dead slot from a student mid-lab, because the capstone's own destroy->rebuild
   gap can exceed the 15-min idle timeout. Her fix: filter on Firebase identity FIRST.

### BUG-077 — P1 -> P3 -> **P2**. I filed a P1 on my own tooling's debris.
`fail()` used `process.exit(1)`, which skips `finally`, so failing runs skipped their own
cleanup and left a baseline that the NEXT run read as a product defect. Clean re-run =
**GATE PASSED** (every cheat rejected, walkthrough 4/4 twice from run 1's leftovers, all four
checks discriminate). Nancy then rejected my P3 as an overcorrection and named the bias. The
real residual: baselines are uid-keyed with no attempt scope, so a returning student who skips
re-recording passes "you really rebuilt it" having destroyed nothing.

### FIXED TONIGHT
- `fail()` throws, IIFE catches AFTER finally -- 8 of 14 harnesses (4165af71c, bc1 + repo).
- **All 14 harnesses now use fixed QC identities**; wall was still minting a random one per run
  and permanently binding a slot (8852355b6). Account deletion REMOVED -- deleting a fixed
  identity frees the email and the next run mints a new uid, so the rename alone would not
  have stopped the leak. Verified: proj-adv-qc still signs in with the uid holding student-13.
- **Hub double-render (#279)** -- the capstone rendered TWICE (Labs projection + curated
  Projects card, different copy). `merged()` de-duped within a section only. Curated now wins
  GLOBALLY (7f9d8e7f6). Proven on preview vs prod: anchors 2->1, survivor is the curated card,
  ONLY Labs(27)->Labs(26) changed. **Chris PASS** -- he found `register-capstone-project.js`
  shows the operator lifted it out of Labs deliberately, so curated-wins matches real intent.
  **Nancy review in flight.** NOT deployed.

### #275 P1 INFRA — root-caused
`reclaim-idle-slots.py` is on the WRONG HOST: `claim_service.py` lives on **bc2**. Copied there,
runs correctly. Nancy's Auth cross-check of all 29 bound uids: 11 resolve and every one is a
`@hexworth-smoke.local` QC account; the other 18 resolve to NO Firebase user. The bound pool is
QC debris, not students.

### #277 catalog reachability — 551 candidates, sampled
266 very likely FALSE POSITIVES (matrix 120 / dark-arts 56 / key 39 / code 35 / cloud 16 --
hubs build cards at runtime, invisible to a static scan). 283 SUSPECT (script 102 / web 67 /
shield 50 / ai 26 / eye 23 / forge 15 -- not on their hub at depth 0). Next: walk from the
COURSE page, not the house hub (script's hub blew past a 70-page cap without concluding).

### Task board
- [~] Nancy review on the hub de-dup fix -> then deploy
- [ ] OPERATOR: Firestore capstone entry removal · OPERATOR: reclaim --apply
- [ ] #272 legacy AWS final -- OPERATOR call. Chris BLOCKED option A: 6 of 20 questions test
      material clf-c02 never teaches. Catalog entry contradicts itself (title "Final" vs
      description "practice exam").
- [ ] BUG-077 fix: attempt-scope the baseline (design has a real strand risk; not built)

### Standing hazards
- FOURTEEN bad checks today, one shape: predicting a string instead of reading it. Latest two,
  both caught by others: `querySelectorAll(...)[0]` then asserting about a section I never
  opened; and `basePath || default` where the real basePath is the empty string.
- New tools, all validated on + and - controls: `reachability-walk.js` (now bounded, reports
  INCONCLUSIVE rather than a false dead end), `harness-link-audit.js`,
  `catalog-reachability-audit.js`.
