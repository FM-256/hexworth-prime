# SITREP — live session cursor

> Shorthand brief of what we are working on RIGHT NOW. Overwritten in place at every
> meaningful transition (task start/finish, decision point, pre/post deploy). On session
> start or after a restart/crash: read this FIRST, then the linked memory files for depth.

<!-- AUTO:BEGIN (regenerated by update-sitrep-auto.sh via Stop hook; do not hand-edit) -->
**Auto-updated:** 2026-07-31 20:46 EDT | branch: master | dirty files: 42

### Latest commits (auto)
- 02e73231b feat(eduscan): a lab that is built and gated must also be reachable
- b31cad127 feat(eduscan): report which orphans are NEW, not just how many there are
- 684bde73a docs(bugs): BUG-077 [P1] capstone check 27 passes on a stale baseline
- 6afa45308 docs(sitrep): lightning shipped+verified; capstone orphan found, Nancy PAUSE answered by bc1 read
- 558db1349 docs(projects): P1 is MIXED-TIER graded, not fully server-side
<!-- AUTO:END -->

**Manual section updated:** 2026-08-01 — capstone gate FAILED; Nancy found my own gate run manufactured the evidence. Re-running clean.

## NOW — BUG-077 [P1]. Capstone link PAUSED. Clean gate re-run IN FLIGHT.

### SHIPPED EARLIER AND VERIFIED ON PRODUCTION (4abd487a3)
Hub lightning + birds out + `/houses/hub/**` cache -> `no-store`. Storm delta 0.0735 vs WCAG
0.10 (27% margin), scoping 18/18, contrast 6/6 AA, perf 60.2fps. BUG-075 + BUG-076 live.

### BUG-077 [P1] — check 27 accepts a STALE baseline
Baselines in bc1 `/app/data/capstone-baselines.json` are keyed by UID ONLY and never expire.
Check 27 asks "does any live id appear in the baseline" but never WHICH ATTEMPT it belongs to.
A returning student with a prior baseline builds fresh, passes "you really rebuilt it", and
never destroyed anything.

**EXPOSURE — I got this wrong once.** I claimed production had no route. FALSE: the catalog
entry (`e608ebd47`, 10:09) is live and the page is HTTP 200, so catalog search reaches it. The
paused hub link (`1d5a63b1d`, 20:18) was a DIFFERENT route ten hours later. Corrected in the
tracker in place, because BUG-058 documents that exact file misleading a reviewer before.
**Nancy's ruling: PULL the catalog entry until 27 is fixed.** Not yet done -- needs Chris + deploy.

### NANCY: my own gate run MANUFACTURED the evidence
`fail()` called `process.exit(1)`, which in Node skips pending `finally` blocks -- so every
FAILING run skipped its own teardown. It left a container holding slot student-13 for 18+ min
and a stale baseline under the QC uid. The harness was ALSO changed today to a FIXED QC
identity, so that debris is inherited by every later run. Cheat E therefore never tested the
empty-baseline case; it inherited one from 5.5h earlier.
FIXED (4165af71c, bc1 + repo): fail() throws, IIFE catches AFTER finally. 8 of 14 harnesses
(only those with a finally). All 14 syntax-clean. Leaked container destroyed. QC baseline entry
removed (3 non-QC entries left untouched -- deleting a real student's baseline would STRAND them).

### P1 INFRA FOUND WHILE CLEANING (#275)
`reclaim-idle-slots.py` CANNOT RUN -- imports `/home/eq1/openstack-stage1/claim_service.py`,
which does not exist on bc1 (the claim service is on bc2). Nancy confirmed it is in no crontab.
Its own docstring: "walked the pool to student-30 and exhausted it -- real students would get
503." Every QC run permanently consumes a slot and nothing releases it.

### CHRIS: legacy AWS final -- Option A BLOCKED
Read all 20 questions vs clf-ch01/02/03: 6 of 20 (30%) test material the course NEVER teaches
(DocumentDB, VPC Endpoints, OpsWorks, Session Manager, DR/RTO, Kinesis). Also corrected me:
only ch09/ch10 are reachable -- ch06 and ch11 are ALSO searchable-but-unlinked. THREE instances
in one legacy tree, all `status: available`. B/C/D is an operator call.

### Task board
- [~] clean gate re-run -> `/home/eq1/qc-project-clean-2026-08-01.log`. Tests BOTH: does 27 fail
      closed on a genuinely empty baseline, AND does the finally fix clean up after itself.
- [ ] Nancy's ruling: pull the capstone catalog entry (needs Chris + deploy)
- [ ] #275 P1 reclaim-idle-slots.py is dead -> point at bc2, then cron it
- [ ] #276 catalog-reachability standing check (Chris: a defect CLASS, not a cleanup)
- [ ] #272 legacy AWS final -- OPERATOR call (B/C/D); bare C indefensible, catalog entry
      contradicts itself: title "AWS CCP Final Quiz" vs description "practice exam"
- [ ] #269 checks 25/28 forgeable; its "gains nothing" argument leans on 27, which just failed

### Standing hazards
- TWELVE bad checks today, all one shape: predicting a string instead of reading it.
  Latest: `find | head -1` handed me an _archive copy of a lab I was about to edit.
- New tools, both validated on + and - controls: `reachability-walk.js`, `harness-link-audit.js`.
