# SITREP — live session cursor

> Shorthand brief of what we are working on RIGHT NOW. Overwritten in place at every
> meaningful transition (task start/finish, decision point, pre/post deploy). On session
> start or after a restart/crash: read this FIRST, then the linked memory files for depth.

<!-- AUTO:BEGIN (regenerated by update-sitrep-auto.sh via Stop hook; do not hand-edit) -->
**Auto-updated:** 2026-08-01 10:28 EDT | branch: master | dirty files: 51

### Latest commits (auto)
- 3631ec60a fix(dark-arts): feh-09 exercise 1 granted on a bare click; every sibling gates
- 841b34e54 docs(sitrep): 12 labs read, 7 defects fixed in 4 labs, 7 clean
- 895291ee1 fix(pl-300): ch02 task 7 had a condition that could never fail
- f8b32a686 docs(taskboard): pl300-ch05 clean, and the best grading design seen tonight
- b7b4ba9ae feat(eduscan): AST-based grant-guard audit, after five regex attempts failed
<!-- AUTO:END -->

**Manual section updated:** 2026-08-01 — 14 labs read, 8 defects fixed in 5 labs, 9 clean. Everything substantive waits on the operator.

## NOW — grading-honesty sweep. Two newest fixes with Chris; the rest gated and undeployed.

### LIVE ON PRODUCTION (deployed and verified there earlier)
Hub lightning + `/houses/hub/**` cache `max-age=3600` -> `no-store` (was hiding hub changes from
returning students for up to an hour). BUG-075 + BUG-076.

### 8 DEFECTS FIXED, COMMITTED, NOT DEPLOYED
  ms102-ch05  x2  task 01 took any two non-empty strings; task 04 had a SECOND free door.
                  NANCY found door two -- I had fixed only the one I found.
  pl300-ch04  x3  tasks 2/5/6 named a value and took any. THE FILE A HEURISTIC CALLED CLEAN.
  ms900-ch03  x1  task 8 granted on any period, not the "Last 90 days" it names.
  pl300-ch02  x1  guard COULD NEVER FAIL -- (includes('channel') || includes('factsales')) where
                  the branch already required 'sum(factsales[totalamount]'. Region passed a task
                  naming Channel.
  feh-09      x1  credit button shipped WITHOUT `disabled` -- 15 of 16 across feh-06..10 have it.
GATES: Chris PASS + Nancy PROCEED cover the first three labs. The last two landed AFTER those
verdicts and are with Chris now.

### 9 LABS CLEAN, verified not assumed
ms102-ch06/07/08, ms900-ch01, pl300-ch01/03/05, forge-md100-m08, dark-arts feh-06.
Best designs seen, worth copying into anything built next:
  pl300-ch05     unrecognised-but-valid DAX gets output and NO credit (fallback carries no id).
  ms102-ch08     DERIVES the answer from live DOM; a wrong answer RESETS options so it cannot
                 be used to eliminate.
  forge-md100-m08 three sequential guards, and the wrong-action branch TEACHES ("you just
                 allowed outbound traffic on the C2 port") instead of only rejecting.
  feh-06..10     strongest architecture: the credit button ships DISABLED, so there is no
                 bare-click door to leave open by construction.

### METHOD -- the durable finding
All 8 defects came from READING the task text against the grant condition. The four scanners
built tonight found NONE of them, and each lied about its own blind spot until tested against a
known answer. FIVE distinct completion architectures exist across 14 labs; any tool assuming one
reports confidently and wrongly on the rest.
A ONE-LINE grep of a grant is unreliable -- it showed a "bare" markDone four times where the
guard was simply above the printed line. The BLOCK is the unit, not the line.

### WAITING ON THE OPERATOR
1. **Deploy the lab fixes** + the decision they carry: students who already earned these via the
   old free doors keep them (per-student localStorage, no server record, unidentifiable).
   Deploying without answering settles it silently.
2. **Auditor taxonomy** -- project or lab. All 126 project pages are honor-system.
3. **Firestore capstone removal** -- rule 10, needs the operation named.
4. **`reclaim --apply`** -- 29 of 29 bound slots are QC/dead uids, ZERO students. Pool 1 free of 30.
5. **Curriculum** -- legacy AWS final (6 of 20 questions untaught), ~272 unreachable entries,
   31 of them briefed to the AI assistant.

### DESIGNED, MEASURED, NOT BUILT
Cloud Auditor (Nancy PROCEED after 6 rounds; SDK chosen on measurements: 2,321ms vs 19,363ms CLI).
BUG-077 fix (Nova changes-since verifies teardown POSITIVELY; nothing purges those rows).

### Standing hazards
- ONE clean pass + ONE clean fail is not evidence. Try the near-miss and the ABSENT value.
- A guard that cannot fail is not a guard. Ask what input makes the condition false.
- Three of my own fixes reopened the bug they closed. Suspect every guard added for KINDNESS.
- Backticks in `git commit -m` are shell substitution. Ate text three times. Use -F.
