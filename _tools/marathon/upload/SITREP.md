# SITREP — live session cursor

> Shorthand brief of what we are working on RIGHT NOW. Overwritten in place at every
> meaningful transition (task start/finish, decision point, pre/post deploy). On session
> start or after a restart/crash: read this FIRST, then the linked memory files for depth.

<!-- AUTO:BEGIN (regenerated by update-sitrep-auto.sh via Stop hook; do not hand-edit) -->
**Auto-updated:** 2026-08-01 02:08 EDT | branch: master | dirty files: 45

### Latest commits (auto)
- b19dc5e42 fix(pl-300): ch04 accepted the wrong answer on three tasks and confirmed it back
- 40d1e3b14 docs(taskboard): ms900-ch01 audited and cleared -- inline grants are honest there
- 1786dcc43 feat(eduscan): find EVERY path to a task grant, not the first one
- 888fb4c9e fix(ms-102): ch05 told students they can manage Exchange; two tasks verified nothing
- cd77b93dc docs(taskboard): correct my own ms102-ch05 analysis -- 2 real gaps, 1 false alarm
<!-- AUTO:END -->

**Manual section updated:** 2026-08-01 — lab-honesty sweep running. 2 labs fixed, 1 cleared, 1 tool built. Four things still need the operator.

## NOW — auditing the 7 portal-sim labs for tasks that grant credit without checking.

### FIXED AND VERIFIED BY CLICKING (committed, NOT deployed)
- **ms102-ch05** (888fb4c9e). Told students "You can now manage shared mailboxes, mail flow
  rules, quarantine and anti-spam policies" while TWO of those four rested on nothing. Task 01
  named exact values and took any two non-empty strings; Task 04 had a SECOND free door --
  a "Mark Reviewed" button granting the task on a bare click. **Nancy found the second door; I
  had only found the first and would have shipped calling it fixed.**
- **pl300-ch04** (b19dc5e42). THE FILE NANCY'S HEURISTIC RETURNED ZERO FOR, and she warned the
  zero was a blind spot. It was -- different shape entirely (no task rail, JS TASKS array). Three
  tasks named a value and accepted any. Task 6 passed a student on Weekly then displayed
  "configured: Weekly at 23:00" -- confirming the wrong answer while awarding the task.

### CLEARED, NO DEFECT
- **ms900-ch01**. 8 inline `onclick=markDone` grants, ALL HONEST -- its tasks say "click the Add
  User button to explore", "click View on ANY service". Clicking IS the task there. Opposite
  verdict from ch05 on the same evidence shape.

### BUILT
`_tools/eduscan/task-grant-audit.js` (1786dcc43) -- finds EVERY path to a task grant, not the
first, which is exactly what I missed on ch05. Reports SUSPECTS, never verdicts: a bare grant is
correct when the task asks for something open-ended (ch05 task 03, pl300 task 09 -- I wrongly
flagged the first of those). Two of its own detector bugs found by testing against a known
answer: comment prose counted as grants, and dynamic `markDone(taskNum)` read as "unreachable".

### STILL TO READ, individually
ms102-ch06 / ch07 / ch08 and ms900-ch03. Mostly checkpoint-granted. Each needs task descriptions
read FIRST, then the handler, then every OTHER path to the same markDone.

### WAITING ON THE OPERATOR (none blocked on work I can do)
1. **Auditor taxonomy** -- project or lab. ALL 126 project pages are honor-system click-through;
   this would be the platform's first server-graded project. Nancy: incidental, proceed. Not
   cheaply reversible once the URL exists.
2. **Firestore capstone removal** -- rule 10, needs the operation named.
3. **`reclaim --apply`** -- identity filter BUILT and run: 29 of 29 bound slots are QC accounts or
   dead uids, ZERO real students. Pool at 1 free of 30.
4. **Curriculum**: legacy AWS final (Chris BLOCKED wiring it -- 6 of 20 questions untaught), and
   ~272 unreachable catalog entries, 31 of which the AI assistant is briefed on.

### DESIGNED, MEASURED, NOT BUILT
- **Cloud Auditor project** -- Nancy PROCEED after 6 rounds. SDK chosen (2,321ms vs 19,363ms CLI,
  overrides confirmed, 20/20 payload under 4-way concurrency).
- **BUG-077 fix** -- Nova `changes-since` returns DELETED instances with terminated_at, so the
  teardown can be verified POSITIVELY instead of inferred from absence. No strand risk. Nothing
  purges those rows. Building it is a bc1 server.js change.

### Standing hazards
- The lexical-const trap bit THREE times today in different forms. Top-level `let`/`const` in a
  script block is invisible to `evaluate()` and to other script blocks. Read the DOM instead.
- Measure the PAYLOAD, not the clock, and not the count. Two "wins" tonight were failures wearing
  a fast stopwatch; one "verification" was a number that could not have moved.
