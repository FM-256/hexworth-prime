# SITREP — live session cursor

> Shorthand brief of what we are working on RIGHT NOW. Overwritten in place at every
> meaningful transition (task start/finish, decision point, pre/post deploy). On session
> start or after a restart/crash: read this FIRST, then the linked memory files for depth.

<!-- AUTO:BEGIN (regenerated by update-sitrep-auto.sh via Stop hook; do not hand-edit) -->
**Auto-updated:** 2026-07-31 11:56 EDT | branch: master | dirty files: 44

### Latest commits (auto)
- 056e2ea62 feat(quiz): InstantQuizGrader -- server-held keys with instant per-question feedback
- 47417ffa7 docs(bugs): BUG-067 -- 2 live quizzes passable by clicking option B every time
- f29b74de6 docs(sitrep): deployed + registered; two operator decisions, two new bugs logged
- ed5906294 tools(pool): report-only slot-ownership audit; deliberately has no --apply
- 811160b20 docs(bugs): BUG-066 -- correcting my own false claim; the slot pool still leaks
<!-- AUTO:END -->

**Manual section updated:** 2026-07-31 — BUG-065/067 groundwork committed, page rewrite HELD.

## NOW — PAUSED at a clean checkpoint. Operator changing locations; no new tasks started.

Operator said "we will be changing locations in a bit so do not start any new tasks." Marathon
heartbeats after that point were deliberately NOT actioned — the human instruction is newer and
more specific than the cron. Nothing is half-applied; the tree is safe to walk away from.

### RESUME HERE — BUG-065/067, one command from applying
Committed `056e2ea62`, all four files ADDITIVE and inert (no page references them yet):
`InstantQuizGrader.js` · `_tools/instant-quiz-grader-test.js` · `_tools/patch-openstack-quizzes.js`
· `_docs/operations/instant-quiz-grader-design-2026-07-31.md`.

Harness is **ablation-proven**: normal build PASS (200 seeds x 4 questions incl. duplicate-text
options); ablated build (off-by-one injected into the display->original crossing) **5668
failures**; shuffle distribution 24/26/26/25% over 4000 runs. It can fail, so its pass means
something. `patch-openstack-quizzes.js` is **dry-run clean on all 4 files**, not applied.

**Why NOT applied — this is the load-bearing sequencing constraint, not caution:** once the
pages call `gradeQuiz` they depend on `quiz_keys/{id}` existing in Firestore. Apply now and any
unrelated hosting deploy scores every student 0/15 (CLAUDE.md rule 9). Seed and pages ship
together or not at all. Correct order:
1. operator go/no-go on the production Firestore write (STILL UNANSWERED — see below)
2. `node functions/seed-openstack-quiz-keys.js --apply` (keys carry `revealToAll: true`)
3. `cd functions && node verify-quiz-keys.js <each of 4>` -> `Verification PASSED`
4. `node _tools/patch-openstack-quizzes.js --apply` (archives originals first)
5. Nancy -> Chris -> `./deploy.sh`

**Two things found by reading rather than assuming, already baked into the patch:**
`gradeQuiz` returns **`percentage`**, not `score` (`score` is the raw count — using it would
have shown students 11% instead of 73%). And `gradeQuiz` **rejects unauthenticated callers**
while these 4 quizzes work signed-out TODAY, so the rewrite checks auth *before* question 1 and
shows a sign-in panel, instead of letting a student answer all 15 and have none of them count.

### BLOCKING QUESTION — asked, never answered
I read "fix the 4 quizzes" as authorizing the production `quiz_keys` seed and flagged that
reading explicitly. No answer came. Not proceeding on my own reading of a production write.

---

## Earlier today — DEPLOYED + capstone REGISTERED

Nine defects shipped and verified on production today. Capstone live on the Cloud Master hub.

| Was | Now (measured on production) |
|-----|------------------------------|
| shield incubator **108 dead links of 111** | **167 / 167, zero dead** |
| Rescue lab **unlaunchable for every student** | seed fixed, page ships clean-start guidance |
| returning students told "cannot be completed" | resume carries `cloudMode` |
| Dr. Hex `grade-for` hid **16 cloud checks** | reads CLOUD_CHECKS, refuses to claim `complete` when ungraded |
| capstone check 27 forgeable AND impassable | grader-held baseline, gate green |
| counters advertised unreachable totals | "47 of 58 built" / "3 of 26 built" |
| `grep`/`wc` silently passed on missing files | report properly, ablation-proven harness |

**Capstone registered:** `hubRegistry/cloud-master` `sections.projects` 0 -> 1. Renders TWICE
and clickable — Labs(27) via ContentCatalog projection, Projects(1) via the Firestore entry.

### OPEN — with the operator
1. **Cinder lab (BUG-058).** The claim "the volume outlived its first server" is UNPROVABLE
   from live state: Cinder keeps no attachment history (`/volume/v3/attachments` -> 404) and
   Nova empties `volumes_attached` on delete (both measured). Every fix failed on that. The one
   sound implementation (seed the first server) makes lab 1 IDENTICAL to the Rescue lab — Chris
   called it "a costume on the first one" and ruled RETIRE. Curriculum call, not mine.
   Integrity defect only: nobody blocked, nobody loses work.
2. **QC slot debris.** 30 slots / 27 bound / 3 free; 26 live servers named exactly what the
   harnesses create. Report-only audit built and committed (`dump-slot-uids.py` +
   `audit-pool-slot-owners.js`, NO --apply). **Blocked on credentials:** the definitive check
   (does the bound uid still exist in Firebase Auth) returns `auth/internal-error` — this
   credential reaches Firestore but not Identity Toolkit. Secondary signal, verified meaningful:
   **0 of 27** bound slots has a `users/{uid}` profile doc. Consistent with debris; still a
   proxy, so nothing deleted.

### NEW — logged today, NOT actioned
**BUG-065 (P1):** 4 LIVE OpenStack quizzes ship answer keys in page source — measured on
production, 15 `correct:` fields each, no `gradeQuiz`/`serverGrading`/`quizId`. Fixing means
seeding `quiz_keys` in production Firestore (rule 9), so it needs authorization.
QC-57's "95 client-graded quizzes" headline is STALE: 488 of 496 are server-graded, 4 are not.
**BUG-066 (P2):** my "fixed QC identity" fix only SLOWED the slot leak. Firebase purges
`@hexworth-smoke.local` accounts (3 of 12 survive), so each run rebinds a new slot. I stated
otherwise earlier today; corrected in the tracker.

### THE RULE THIS SESSION KEEPS PROVING — could this check have failed?
Eight times a green signal measured something adjacent to the claim: "0 modules lost" counted
card PRESENCE not reachability (290 links died while it passed) · "no href equals #" missed 6
cards pointing at nothing · "the cheat was rejected" hid a check that rejected EVERYONE · "the
target 404s" is not "clicking 404s" (I overstated BUG-063 by 34x) · a FIX can be a silent no-op
(`-lab` vs `.lab`) · **A HARNESS CAN BE A FALSE ORACLE** (my first grep test passed against a
deliberately ABLATED engine) · a shell `${}` made a LIVE fix look missing · a uniform 0-of-27
result is a detector smell until you prove the collection is the right one.
**Check the detector before the data.** My own detectors were wrong 5+ times today.

### #103 RE-SCOPED by Nancy — do not re-litigate
arm-sql BLOCKED behind held branch `armsql-honesty-wip` + a content rebuild (11/12 worked
examples print FABRICATED output). arm-bash "misses" are PINNED decisions with the reasons
written directly above the lines. dark-arts AUDITED CLEAN (all 13 gate on correct-only, none
award ModuleProgress).

### SWEEPS (bounding the damage — clean beyond the above)
Broken links across 5,220 `_app` pages: 13 real (11 unfixable, content absent) · inflated
denominators: only the 2 fixed · quiz-key bridge **615/615 zero gaps** · LinuxTerminal
fs-reading handlers: only grep+wc were silent.

### THE RECIPE THAT MAKES GATES PASS FIRST TIME
1. EXTEND /verify on bc2 first, then **probe the payload against a REAL resource** before
   writing any check. Three separate bad fields were caught this way (flavor/addresses
   missing entirely; `owned` counting the admin's external net as the student's). Lab 2
   skipped this step and shipped uncompletable.
2. Add CLOUD_CHECKS on bc1. **server.js is BAKED INTO THE IMAGE** — host edits do nothing.
   `docker compose build lab-manager && docker compose up -d lab-manager`, then verify
   INSIDE the container. Back up + diff first (expect `0 removed`).
3. Clone the newest lab page (monitor wiring is already reviewed), swap ids//content.
4. Two harnesses, VERBATIM from the page. Adversarial cheats must assert what should still
   PASS as well as what must fail — a check that rejects everything is broken, not strict.
5. `qc-lab.sh <mode>`. Adversarial first; walkthrough runs TWICE with no cleanup between.
6. Catalog entry BY HAND. Copy-paste put HexToken's description on Gate 5.
7. `bash _tools/deploy/is-it-live.sh` after deploying. "Not deployed" is a claim.

### HARD-WON, DO NOT RELEARN
- **`docker compose up -d` DOES NOT ALWAYS RECREATE THE CONTAINER.** Build succeeded, image
  contained the fix, container kept running the OLD file. Three consecutive QC gate runs
  tested stale code and I "diagnosed" the same symptom three different ways because of it.
  Use `docker compose up -d --force-recreate lab-manager`, then PROVE it:
      sha256sum ~/hexworth-sandbox/lab-manager/server.js
      docker exec lab-manager sha256sum /app/server.js
  Matching shas or the deploy did not happen. Never suppress build output with >/dev/null.
- **CLOUD_CHECKS entries are evaluated ONLY as `fn(state, seed)`.** A `cmd:` entry there
  fails unconditionally (fn is undefined -> throws -> caught -> pass=false). cmd checks
  belong in SANDBOX_CHALLENGES, where execCheck runs them. Symptom when wrong: cheats look
  "correctly rejected" because the check rejects EVERYTHING, and only a should-pass case
  exposes it.
- **`id: 12` (and others) exist in BOTH CLOUD_CHECKS and SANDBOX_CHALLENGES.** Anchoring an
  edit on an id pattern matches the FIRST block. A move anchored that way silently
  re-inserted the checks into the block they came from. Anchor on line ranges verified
  against the block boundaries, then verify by POSITION relative to
  `const SANDBOX_CHALLENGES`, not by "the id is present somewhere".

- **Docker exec output is FRAMED, even with `Tty: true`.** Reading an exec stream raw gives you
  `[1 byte type][3 pad][4 byte BE length]` glued to the front of the payload, so `JSON.parse`
  throws and you conclude the file is missing when it is sitting right there. Measured on the
  real image: `Tty=true` and `Tty=false` produced byte-identical framed output
  (` ...{"manifest_version":1}`). Use `docker.modem.demuxStream(stream, out, err)`.
  `execCheck` never noticed because it only reads an exit code and discards stdout — so the
  first thing in this codebase that actually needed OUTPUT hit it. Cost one full gate cycle;
  the diagnosis came from dumping `od -c` of the real stream, not from reasoning about it.
- `systemctl is-active` is NOT a health check. I took the bridge down 4 min with a
  NameError; service showed active, journal clean, every /verify returned 502. CALL THE
  ENDPOINT.
- Neutron: `openstack router delete` refuses while an interface is attached -> network
  undeletable -> next run makes a DUPLICATE -> every command naming it is ambiguous and the
  student is stuck forever. `router remove subnet` before delete, in the page AND harness.
- Nova needs `X-OpenStack-Nova-API-Version: 2.47` or flavor comes back nameless.
- Per-student RAM quota 192MB; anything above m1.nano is refused at create time.
- Two shared networks exist so `--network` is mandatory (HTTP 409 otherwise).
  **Re-run `ensure-second-network.sh` after every DevStack rebuild** or lab 2 regresses.
- NEVER chain `git commit` behind a scripted edit. Three false commit messages and one
  committed syntax error came from a verification step whose failure did not block the write.

### CLOUD MASTER PROJECT 1 — "The Environment Is Data" (in progress)
Design: `_docs/architecture/cloud-master-project-manifest.md`. Grader ids 25-28 LIVE on bc1.
Arc: export -> destroy -> rebuild from the manifest alone -> prove different resource IDs.
The anti-cheat IS the lesson: same shape, different identity.

CONSTRAINTS VERIFIED (do not re-derive):
- **Heat is NOT installed** (nova, neutron, cinder, glance, keystone, placement only), so
  OpenStack-native orchestration is off the table. IaC has to come from the client side.
- **Student image has NO terraform / ansible / yq / git.** It has openstack, python3, jq, curl.
- **The grader has NO `files` object.** Evidence checks use `cmd:` (shell inside the
  container); cloud checks use `fn:` (server-side /verify). I nearly wrote checks against a
  `files` arg that does not exist -- the Lab 2 defect exactly. Checks 25/27/28 are `cmd:`,
  26 is `fn:`, deliberately mixed so forging a file alone gains nothing.

REMAINING for P1: the page, plus two harnesses + a `project` mode in qc-lab.sh.

### FILED, NOT SCHEDULED — Terraform Hub + open-world composability
`~/hexworth-shared/Planning/Terraform-Hub/00-IDEA-AND-EVIDENCE.md`
Terraform DOES exist in Hex (`_app/houses/code/labs/code-terraform.lab.html` + a tool) but
is a **reading page** -- 0 SandboxLauncher hooks, 0 validators, nothing executes. 447 files
mention it in prose. Frank's open-world instinct is right: the sandboxes are real, so a
student container CAN reach a real cloud API. Adding the terraform binary to the
openstack-cli image would give real `terraform apply` against their OWN tenancy. One
Dockerfile line plus a lab. Deliberately NOT folded into P1 (would make it "learn
Terraform" instead of "understand reproducibility"); it belongs at P3.

### OPEN — operator calls
1. 31 standalone CLH labs have no Observatory telemetry (0 of 31). Deliberate or gap?
2. #250 routing, #253 NOPASSWD sudo, #255 /verify rate-limit.
3. Probe adoption: mechanism VERIFIED in real Pyodide, no lab converted yet.
