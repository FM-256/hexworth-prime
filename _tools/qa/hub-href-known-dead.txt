# KNOWN-DEAD LINKS — currently EMPTY, and that is the correct state.
#
# Read by _tools/qa/hub-href-integrity-test.js.
#
# ⚠ THIS FILE ONCE HELD 42 ENTRIES THAT DID NOT BELONG IN IT. They were ContentCatalog entries
# marked `status: 'coming-soon'` — roadmap placeholders for Forge "Intro to Computers" and the
# Shield PIS chapter aliases. I treated "the href does not resolve" as the defect, when the real
# question is whether anything can FOLLOW the href. Nothing can: HouseRenderer.openModule()
# (:1864) alerts "coming soon" rather than navigating, and ContentCatalog.search() defaults to
# `status: 'available'` (:4891), as do ContentDiscovery, ProgressManager, XPMasterLedger and
# CompletionStamp. Browser-verified: 45 coming-soon entries, ZERO reachable as a clickable link.
# So the gate now filters on status and there is nothing left to baseline.
#
# The lesson worth keeping: a baseline is where a measurement error goes to look permanent.
# Before adding a line here, prove the link is REACHABLE by a student, not merely unresolvable.
#
# Format: one path relative to _app/, one per line.
