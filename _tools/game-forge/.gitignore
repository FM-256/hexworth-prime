# Staging output of generate_edition.mjs — must NEVER be committed or deployed.
# (Already covered by the blanket _tools/ ignore; this is explicit defense-in-depth
#  in case _tools/game-forge/ is ever un-ignored to track tooling without `git add -f`.)
editions-staged/
_map_*.json
