{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "nav.session_start",
  "title": "Session Start",
  "description": "Emitted on first event of a new behavioral session (page load with no prior sessionId in localStorage, OR after a >2min token-expiry gap).",
  "type": "object",
  "additionalProperties": false,
  "required": ["entryUrl"],
  "properties": {
    "entryUrl": {
      "type": "string",
      "description": "URL the student landed on at session start"
    },
    "referrer": {
      "type": "string",
      "description": "document.referrer if available"
    },
    "consentVersion": {
      "type": "string",
      "description": "Active consent version at session start (e.g., 'v2.0')"
    }
  },
  "governance": {
    "status": "instructor-facing",
    "consentScope": "interaction data",
    "phase": 1,
    "dimension": 1
  }
}
