{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "item.start",
  "title": "Content Item Started",
  "description": "Emitted when a student begins engaging with a specific content item (module, quiz, lab, video). First view OR resumed view both emit this.",
  "type": "object",
  "additionalProperties": false,
  "required": ["itemId", "itemType"],
  "properties": {
    "itemId": {
      "type": "string",
      "description": "Stable content identifier (e.g., 'fw-w1-logical-quiz', 'mod-net-osi-layers')"
    },
    "itemType": {
      "type": "string",
      "enum": ["module", "quiz", "lab", "video", "reading", "presentation", "applet", "exam", "ctf"],
      "description": "Content type taxonomy"
    },
    "itemTitle": {
      "type": "string",
      "description": "Display title at time of access (denormalized for analytics)"
    },
    "isResume": {
      "type": "boolean",
      "description": "True if the student has previously started this item; false on first access"
    },
    "courseContext": {
      "type": "string",
      "description": "Course / chapter / unit identifier this item belongs to"
    }
  },
  "governance": {
    "status": "instructor-facing",
    "consentScope": "interaction data, performance data",
    "phase": 1,
    "dimension": 2
  }
}
